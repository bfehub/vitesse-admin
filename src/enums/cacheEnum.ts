// token key
export const TOKEN_KEY = 'TOKEN__KEY'

// user info key
export const USER_INFO_KEY = 'USER__INFO__KEY'

// project config key
export const PROJECT_CONFIG_KEY = 'PROJECT__CONFIG__KEY'

// cache type
export enum CacheTypeEnum {
  LOCAL,
  SESSION,
}
