/**
 * @description 主题模式
 */
export enum ThemeEnum {
  DARK = 'dark',
  LIGHT = 'light',
}

/**
 * @description 权限模式
 */
export enum PermissionModeEnum {
  // 前端模式
  ROLE = 'ROLE',
  // 后端模式
  BACK = 'BACK',
}
